{
  "name": "erw-suite",
  "version": "0.1.0",
  "private": true,
  "description": "Enterprise Resilience Workout suite — MVP. Pre-built core (dataset, contracts, derive, export-kit, DB schema) + Replit build brief.",
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "dev": "echo 'App not scaffolded yet — see replit.md and docs/replit-build-brief.md (slice 1 adds the dev server)' && exit 1"
  },
  "dependencies": {
    "drizzle-orm": "^0.44.0",
    "zod": "^3.25.0"
  },
  "devDependencies": {
    "typescript": "^5.8.0",
    "vitest": "^3.2.0"
  }
}
